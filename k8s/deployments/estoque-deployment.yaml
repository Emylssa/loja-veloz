
apiVersion: apps/v1
kind: Deployment
metadata:
  name: estoque
spec:
  replicas: 2
  selector:
    matchLabels:
      app: estoque
  template:
    metadata:
      labels:
        app: estoque
    spec:
      containers:
        - name: estoque
          image: estoque:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: app-config
